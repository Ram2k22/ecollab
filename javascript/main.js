const blur = document.querySelector('.blur'),
blurButton = document.querySelector('.blur button'),
popUp = document.querySelector('.blur .pop-up');

blurButton.addEventListener('click' , (event) => {
    blur.classList.add('show');
}
)


